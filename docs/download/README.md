Place the latest Processing Library Manager submission files here.

Expected published URLs:

- `https://jshaw.github.io/Canvas2DMX/download/canvas2dmx.zip`
- `https://jshaw.github.io/Canvas2DMX/download/canvas2dmx.txt`

These files are produced by the Gradle release build:

```bash
./gradlew clean buildReleaseArtifacts packageRelease stageContributionArtifactsToDocs
```

After running that command, commit the updated files in this folder so GitHub Pages can host them.
